spring.jpa.database=POSTGRESQL
spring.datasource.platform=postgres
spring.datasource.url=jdbc:postgresql://localhost:5433/postgres
spring.datasource.username=postgres
spring.datasource.password=ecommerce-db

# Connected to gcp from local using cloud-sql-proxy
# https://stackoverflow.com/questions/60104672/pgadmin4-connect-to-postgres-in-google-cloud-sql
# https://cloud.google.com/sql/docs/postgres/connect-auth-proxy#tcp-sockets

#to display the sql quering generated using jpa
spring.jpa.show-sql=true

#log
#logging.level.org.springframework = debug

# available options are update, validate, create, create-drop
spring.jpa.hibernate.ddl-auto=update